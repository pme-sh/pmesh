builds:
  - id: pmesh
    goos: [windows, linux, darwin]
    goarch: [amd64, arm64]
    env:
      - CGO_ENABLED=0

archives:
  - format: zip
    id: pmesh
    builds: [pmesh]

checksum:
  algorithm: sha256
